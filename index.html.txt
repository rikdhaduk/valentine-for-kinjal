<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>For Kinjal ❤️</title>
  <style>
    body {
      font-family: system-ui, sans-serif;
      background: #ffe6f2;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      margin: 0;
    }
    .card {
      background: white;
      padding: 24px 28px;
      border-radius: 18px;
      box-shadow: 0 10px 25px rgba(0,0,0,0.1);
      text-align: center;
      max-width: 360px;
    }
    h1 {
      margin-top: 0;
      color: #ff4d88;
    }
    .question {
      font-size: 1.1rem;
      margin: 18px 0 24px;
    }
    .btn-row {
      display: flex;
      justify-content: center;
      gap: 12px;
      flex-wrap: wrap;
    }
    button {
      border: none;
      padding: 10px 18px;
      border-radius: 999px;
      background: #ff4d88;
      color: white;
      font-size: 0.95rem;
      cursor: pointer;
      transition: transform 0.15s ease, box-shadow 0.15s ease, background 0.15s;
    }
    button:hover {
      background: #ff336f;
      transform: translateY(-1px);
      box-shadow: 0 4px 10px rgba(0,0,0,0.15);
    }
    .hidden {
      display: none;
    }
    .small {
      font-size: 0.85rem;
      color: #777;
      margin-top: 10px;
    }
  </style>
</head>
<body>
  <div class="card">
    <h1>Hi Kinjal 💌</h1>

    <!-- Step 1 -->
    <div id="step1">
      <div class="question">
        Question 1:<br>
        Do you like spending time with me?
      </div>
      <div class="btn-row">
        <button onclick="nextStep(2)">Of course 💖</button>
        <button onclick="nextStep(2)">Yes 😌</button>
      </div>
    </div>

    <!-- Step 2 -->
    <div id="step2" class="hidden">
      <div class="question">
        Question 2:<br>
        Do you know how special you are to me?
      </div>
      <div class="btn-row">
        <button onclick="nextStep(3)">I have a hint 😊</button>
        <button onclick="nextStep(3)">Tell me more ✨</button>
      </div>
    </div>

    <!-- Final question -->
    <div id="step3" class="hidden">
      <div class="question">
        Final question:<br>
        Kinjal, will you be my valentine?
      </div>
      <div class="btn-row">
        <button onclick="sayYes()">Yes 💘</button>
        <button id="noBtn" onclick="runAway()">No 🙈</button>
      </div>
      <div class="small" id="hint"></div>
    </div>

    <!-- Result -->
    <div id="result" class="hidden">
      <div class="question">
        Yaaay! 🥹💞<br>
        Thank you, Kinjal. You just made my day.
      </div>
    </div>
  </div>

  <script>
    function nextStep(n) {
      document.querySelectorAll('#step1, #step2, #step3').forEach(el => el.classList.add('hidden'));
      document.getElementById('step' + n).classList.remove('hidden');
    }

    function sayYes() {
      document.querySelectorAll('#step1, #step2, #step3').forEach(el => el.classList.add('hidden'));
      document.getElementById('result').classList.remove('hidden');
    }

    function runAway() {
      const noBtn = document.getElementById('noBtn');
      const hint = document.getElementById('hint');
      const card = document.querySelector('.card');
      const rect = card.getBoundingClientRect();

      const maxX = rect.width - 100;
      const maxY = rect.height - 60;
      const x = Math.random() * maxX;
      const y = Math.random() * maxY;

      noBtn.style.position = 'relative';
      noBtn.style.left = x + 'px';
      noBtn.style.top = y + 'px';

      hint.textContent = "Nice try, Kinjal… the correct answer is the pink one 😉";
    }
  </script>
</body>
</html>
